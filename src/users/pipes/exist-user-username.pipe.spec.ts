import { ExistUserUsernamePipe } from './exist-user-username.pipe';

describe('ExistUserUsernamePipe', () => {
  it('should be defined', () => {
    expect(new ExistUserUsernamePipe()).toBeDefined();
  });
});
